{
  "name": "hams-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm run start --prefix backend",
    "build": "npm run build --prefix backend",
    "postinstall": "npm run postinstall --prefix backend && npm install --prefix backend"
  }
}
